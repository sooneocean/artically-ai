// ComfyUI client (placeholder)
// TODO: call ComfyUI API with workflow JSON

async function runComfyUI(baseUrl, workflowPath) {
  return {
    ok: true,
    note: 'ComfyUI integration placeholder',
    baseUrl,
    workflowPath
  };
}

module.exports = { runComfyUI };
