# ComfyUI 整合（預留）

## 用途
- 可用於產出「主視覺補強圖」或「多版視覺提案」

## 設定
- `config/config.example.json`
  - `comfyui.enabled=true`
  - `comfyui.base_url` 指向你的 ComfyUI
  - `comfyui.workflow` 放置 ComfyUI JSON

## 注意
目前為 placeholder，實際呼叫將於你提供 workflow 後補上。
